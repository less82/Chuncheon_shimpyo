# 두 버전(어른용/일반) + 모바일 UI 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 시민앱을 어른용(즐겨찾기 중심 큰 카드/리스트)과 일반(지도 중심) 두 버전으로 나누고, 언제든 전환 가능한 모바일 앱 UI로 만든다.

**Architecture:** `useViewMode` zustand 스토어(`'elder'|'normal'`, 기본 elder, localStorage) 하나로 시민 라우트를 분기한다. 데이터 로직(`useStops`·`useFavorites`·`arrivals`·`walking`)과 3상태 원자(`FacilityBadge`·`facilityText`)는 공유하고 배치만 다르게 한다. 앱 전체를 폰 폭 셸(`AppShell`)로 감싼다(행정 대시보드·인쇄는 제외).

**Tech Stack:** React 19, TypeScript, react-router-dom v6, zustand, Leaflet, Vitest + @testing-library/react (jsdom, globals).

## Global Constraints

- 3상태 표기: 시설은 있음/없음/미확인만. 근거 없이 `status:"no"` 금지(기본 unknown). 항상 아이콘+한글 병기.
- 데이터 기준일/출처 표기 유지(`FacilityBadge`의 `sourceBadge`가 담당 — 그대로 재사용).
- 기능 독립성: 날씨·도착·GPS 실패해도 지도·시설 표시는 동작(폴백 즉시, 스피너 금지).
- 테스트: 신규 로직·컴포넌트는 TDD. `cd app && npm test` 는 항상 green. 커밋은 자주.
- 스타일: 기본 폰트 ≥18px, 터치타깃 ≥48px(토큰 `--touch`), 토큰(`styles/tokens.css`)만 사용.
- 범위 밖: `/admin`(Dashboard), `/print/:id`(PrintPoster) 는 손대지 않는다.
- 모든 명령은 `app/` 디렉터리에서 실행한다(`cd /home/hyeonmin7507/projects/chuncheon/app`).

---

### Task 1: `useViewMode` 스토어

**Files:**
- Create: `app/src/store/useViewMode.ts`
- Test: `app/src/store/useViewMode.test.ts`

**Interfaces:**
- Consumes: `zustand`.
- Produces: `useViewMode` (zustand store), `type ViewMode = "elder" | "normal"`, `VIEW_MODE_KEY: string`. 상태: `mode: ViewMode`, `setMode(m: ViewMode): void`, `toggle(): void`.

- [ ] **Step 1: 실패하는 테스트 작성** — `app/src/store/useViewMode.test.ts`

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { useViewMode, VIEW_MODE_KEY } from "./useViewMode";

beforeEach(() => {
  localStorage.clear();
  useViewMode.setState({ mode: "elder" });
});

describe("useViewMode", () => {
  it("기본값은 elder 다", () => {
    expect(useViewMode.getState().mode).toBe("elder");
  });

  it("toggle 은 elder↔normal 을 왕복한다", () => {
    useViewMode.getState().toggle();
    expect(useViewMode.getState().mode).toBe("normal");
    useViewMode.getState().toggle();
    expect(useViewMode.getState().mode).toBe("elder");
  });

  it("setMode 결과가 localStorage 에 영속된다", () => {
    useViewMode.getState().setMode("normal");
    expect(localStorage.getItem(VIEW_MODE_KEY)).toBe("normal");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd app && npx vitest run src/store/useViewMode.test.ts`
Expected: FAIL — "Failed to resolve import ./useViewMode".

- [ ] **Step 3: 구현** — `app/src/store/useViewMode.ts`

```ts
import { create } from "zustand";

// 화면 버전 = 어른용(큰 카드/리스트) | 일반(지도 중심). localStorage 영속. 기본은 어른용.
export const VIEW_MODE_KEY = "swimpyo:viewMode";

export type ViewMode = "elder" | "normal";

function readMode(): ViewMode {
  try {
    return localStorage.getItem(VIEW_MODE_KEY) === "normal" ? "normal" : "elder";
  } catch {
    return "elder";
  }
}

function writeMode(mode: ViewMode): void {
  try {
    localStorage.setItem(VIEW_MODE_KEY, mode);
  } catch {
    /* 저장 실패는 조용히 무시(프라이빗 모드 등) */
  }
}

interface ViewModeState {
  mode: ViewMode;
  setMode: (m: ViewMode) => void;
  toggle: () => void;
}

export const useViewMode = create<ViewModeState>((set, get) => ({
  mode: readMode(),
  setMode: (m) => {
    writeMode(m);
    set({ mode: m });
  },
  toggle: () => {
    const next: ViewMode = get().mode === "elder" ? "normal" : "elder";
    writeMode(next);
    set({ mode: next });
  },
}));
```

- [ ] **Step 4: 통과 확인**

Run: `cd app && npx vitest run src/store/useViewMode.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: 커밋**

```bash
git add app/src/store/useViewMode.ts app/src/store/useViewMode.test.ts
git commit -m "feat: 화면 버전(어른용/일반) 스토어 useViewMode"
```

---

### Task 2: `VersionToggle` 버튼

**Files:**
- Create: `app/src/components/VersionToggle.tsx`, `app/src/components/VersionToggle.css`
- Test: `app/src/components/VersionToggle.test.tsx`

**Interfaces:**
- Consumes: `useViewMode` (Task 1).
- Produces: `VersionToggle` default export (프롭 없음).

- [ ] **Step 1: 실패하는 테스트 작성** — `app/src/components/VersionToggle.test.tsx`

```tsx
import { describe, it, expect, beforeEach } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";
import VersionToggle from "./VersionToggle";
import { useViewMode } from "../store/useViewMode";

beforeEach(() => {
  localStorage.clear();
  useViewMode.setState({ mode: "elder" });
});

describe("VersionToggle", () => {
  it("어른용이면 '일반' 라벨을 보인다", () => {
    render(<VersionToggle />);
    expect(screen.getByRole("button")).toHaveTextContent("일반");
  });

  it("일반이면 '큰글씨' 라벨을 보인다", () => {
    useViewMode.setState({ mode: "normal" });
    render(<VersionToggle />);
    expect(screen.getByRole("button")).toHaveTextContent("큰글씨");
  });

  it("클릭하면 모드가 전환된다", () => {
    render(<VersionToggle />);
    fireEvent.click(screen.getByRole("button"));
    expect(useViewMode.getState().mode).toBe("normal");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd app && npx vitest run src/components/VersionToggle.test.tsx`
Expected: FAIL — 모듈 해석 실패.

- [ ] **Step 3: 구현** — `app/src/components/VersionToggle.tsx`

```tsx
import { useViewMode } from "../store/useViewMode";
import "./VersionToggle.css";

// 버전 전환 버튼. 어른용일 때 "일반"으로, 일반일 때 "큰글씨"로 바꾼다.
export default function VersionToggle() {
  const mode = useViewMode((s) => s.mode);
  const toggle = useViewMode((s) => s.toggle);
  const toElder = mode === "normal";
  return (
    <button
      type="button"
      className="vtoggle"
      onClick={toggle}
      aria-label={toElder ? "큰 글씨 화면으로 바꾸기" : "일반 화면으로 바꾸기"}
    >
      {toElder ? "큰글씨" : "일반"}
    </button>
  );
}
```

`app/src/components/VersionToggle.css`:

```css
.vtoggle {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: var(--touch);
  padding: 0 var(--sp-4);
  border: 2px solid var(--brand);
  border-radius: var(--radius-pill);
  background: var(--surface);
  color: var(--brand);
  font-size: var(--fs-base);
  font-weight: 800;
  cursor: pointer;
  box-shadow: var(--shadow-card);
}
.vtoggle:active {
  background: var(--brand-soft);
}
```

- [ ] **Step 4: 통과 확인**

Run: `cd app && npx vitest run src/components/VersionToggle.test.tsx`
Expected: PASS (3 tests).

- [ ] **Step 5: 커밋**

```bash
git add app/src/components/VersionToggle.tsx app/src/components/VersionToggle.css app/src/components/VersionToggle.test.tsx
git commit -m "feat: 버전 전환 버튼 VersionToggle"
```

---

### Task 3: 모바일 셸(`AppShell` + `ShellLayout`) + 데스크톱 사이드패널 제거

**Files:**
- Create: `app/src/components/AppShell.tsx`, `app/src/components/AppShell.css`, `app/src/components/ShellLayout.tsx`
- Test: `app/src/components/AppShell.test.tsx`
- Modify: `app/src/features/citizen/CitizenHome.css` (하단 `@media (min-width: 640px)` 블록 삭제)

**Interfaces:**
- Consumes: `react-router-dom` (`Outlet`).
- Produces: `AppShell` default export (`{ children: ReactNode }`), `ShellLayout` default export (라우트 레이아웃, `<Outlet/>` 감쌈).

- [ ] **Step 1: 실패하는 테스트 작성** — `app/src/components/AppShell.test.tsx`

```tsx
import { describe, it, expect } from "vitest";
import { render } from "@testing-library/react";
import AppShell from "./AppShell";

describe("AppShell", () => {
  it("자식을 app-shell 컨테이너로 감싼다", () => {
    const { container } = render(
      <AppShell>
        <p>내용</p>
      </AppShell>,
    );
    const shell = container.querySelector(".app-shell");
    expect(shell).not.toBeNull();
    expect(shell?.textContent).toContain("내용");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd app && npx vitest run src/components/AppShell.test.tsx`
Expected: FAIL — 모듈 해석 실패.

- [ ] **Step 3: 구현**

`app/src/components/AppShell.tsx`:

```tsx
import type { ReactNode } from "react";
import "./AppShell.css";

// 모바일 셸 — 앱을 폰 폭으로 가둬 데스크톱에서도 폰처럼 보이게 한다.
export default function AppShell({ children }: { children: ReactNode }) {
  return <div className="app-shell">{children}</div>;
}
```

`app/src/components/AppShell.css`:

```css
.app-shell {
  position: relative;
  align-self: center; /* #root 는 flex column → 가로 중앙 정렬 */
  width: 100%;
  max-width: 480px;
  min-height: 100dvh;
  background: var(--bg);
}
@media (min-width: 520px) {
  .app-shell {
    box-shadow: 0 0 0 1px var(--border), 0 20px 60px rgba(28, 25, 23, 0.18);
  }
}
```

`app/src/components/ShellLayout.tsx`:

```tsx
import { Outlet } from "react-router-dom";
import AppShell from "./AppShell";

// 시민 라우트 전용 레이아웃 — 폰 폭 셸 안에서 렌더. (행정/인쇄는 이 레이아웃 밖)
export default function ShellLayout() {
  return (
    <AppShell>
      <Outlet />
    </AppShell>
  );
}
```

- [ ] **Step 4: 데스크톱 사이드패널 제거** — `app/src/features/citizen/CitizenHome.css`

파일 맨 끝의 아래 블록을 **삭제**한다(폰 폭 셸 안에서는 뷰포트 기준 사이드패널 전환이 어긋나므로 하단 시트 유지):

```css
@media (min-width: 640px) {
  .home__sheet {
    left: auto;
    right: var(--sp-5);
    bottom: var(--sp-5);
    width: 420px;
    max-height: calc(100dvh - 120px);
  }
  .home__sheet .stopcard {
    border-radius: var(--radius-lg);
  }
}
```

- [ ] **Step 5: 통과 확인**

Run: `cd app && npx vitest run src/components/AppShell.test.tsx`
Expected: PASS (1 test).

- [ ] **Step 6: 커밋**

```bash
git add app/src/components/AppShell.tsx app/src/components/AppShell.css app/src/components/ShellLayout.tsx app/src/components/AppShell.test.tsx app/src/features/citizen/CitizenHome.css
git commit -m "feat: 폰 폭 모바일 셸(AppShell/ShellLayout) + 데스크톱 사이드패널 제거"
```

---

### Task 4: 어른용 홈 로직(`resolvePrimaryStop`, `nearbyStops`)

**Files:**
- Create: `app/src/features/citizen/elderHome.ts`
- Test: `app/src/features/citizen/elderHome.test.ts`

**Interfaces:**
- Consumes: `Stop` (`types/stop`), `haversine`, `LatLng` (`lib/geo`).
- Produces:
  - `resolvePrimaryStop(favIds: string[], stops: Stop[], pos: LatLng): Stop | null`
  - `nearbyStops(stops: Stop[], pos: LatLng, n: number, excludeId?: string): Stop[]`

- [ ] **Step 1: 실패하는 테스트 작성** — `app/src/features/citizen/elderHome.test.ts`

```ts
import { describe, it, expect } from "vitest";
import { resolvePrimaryStop, nearbyStops } from "./elderHome";
import type { Stop } from "../../types/stop";

const mk = (id: string, lat: number, lng: number): Stop => ({
  id,
  stopNo: id,
  name: id,
  lat,
  lng,
  routes: [],
  facilities: {
    shade: { status: "unknown", source: "none" },
    seat: { status: "unknown", source: "none" },
    light: { status: "unknown", source: "none" },
    sign: { status: "unknown", source: "none" },
  },
});

const stops = [
  mk("A", 37.88, 127.73),
  mk("B", 37.95, 127.8),
  mk("C", 37.881, 127.731),
];
const here = { lat: 37.88, lng: 127.73 };

describe("resolvePrimaryStop", () => {
  it("즐겨찾기가 있으면 그 정류장을 고른다", () => {
    expect(resolvePrimaryStop(["B"], stops, here)?.id).toBe("B");
  });
  it("즐겨찾기가 없으면 최근접을 고른다", () => {
    expect(resolvePrimaryStop([], stops, here)?.id).toBe("A");
  });
  it("stops 가 비면 null 이다", () => {
    expect(resolvePrimaryStop(["B"], [], here)).toBeNull();
  });
});

describe("nearbyStops", () => {
  it("가까운 순 n개를 주고 excludeId 는 뺀다", () => {
    const near = nearbyStops(stops, here, 2, "A");
    expect(near.map((s) => s.id)).toEqual(["C", "B"]);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd app && npx vitest run src/features/citizen/elderHome.test.ts`
Expected: FAIL — 모듈 해석 실패.

- [ ] **Step 3: 구현** — `app/src/features/citizen/elderHome.ts`

```ts
import type { Stop } from "../../types/stop";
import { haversine, type LatLng } from "../../lib/geo";

/**
 * 어른용 첫 화면의 "내 정류장" 결정.
 * 1) 즐겨찾기 중 stops 로 해석되는 첫 정류장
 * 2) 없으면 pos(현위치, 호출부가 거부 시 시청 좌표 전달) 최근접
 * 3) stops 가 비면 null
 */
export function resolvePrimaryStop(
  favIds: string[],
  stops: Stop[],
  pos: LatLng,
): Stop | null {
  if (stops.length === 0) return null;
  for (const id of favIds) {
    const fav = stops.find((s) => s.id === id);
    if (fav) return fav;
  }
  return nearbyStops(stops, pos, 1)[0] ?? null;
}

/** pos 기준 가까운 순 정렬 후 상위 n개. excludeId 는 제외. */
export function nearbyStops(
  stops: Stop[],
  pos: LatLng,
  n: number,
  excludeId?: string,
): Stop[] {
  return stops
    .filter((s) => s.id !== excludeId)
    .map((s) => ({ s, d: haversine(pos, { lat: s.lat, lng: s.lng }) }))
    .sort((a, b) => a.d - b.d)
    .slice(0, n)
    .map((x) => x.s);
}
```

- [ ] **Step 4: 통과 확인**

Run: `cd app && npx vitest run src/features/citizen/elderHome.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: 커밋**

```bash
git add app/src/features/citizen/elderHome.ts app/src/features/citizen/elderHome.test.ts
git commit -m "feat: 어른용 홈 로직(내 정류장 결정·근처 목록)"
```

---

### Task 5: `ElderHome` 화면

**Files:**
- Create: `app/src/features/citizen/ElderHome.tsx`, `app/src/features/citizen/ElderHome.css`
- Test: `app/src/features/citizen/ElderHome.test.tsx`

**Interfaces:**
- Consumes: `resolvePrimaryStop`, `nearbyStops` (Task 4), `VersionToggle` (Task 2), `StopCard`(기존 재사용), `MapView`(기존), `useStops`, `useFavorites`, `facilitySummary`, `haversine`, `CITY_CENTER`.
- Produces: `ElderHome` default export (프롭 없음). 재사용: `StopCard` 가 주 정류장 카드(정류장명·4시설 3상태·도착·도보·즐겨찾기·공유·인쇄)를 담당한다.

**설계 메모:** 주 카드는 기존 `StopCard` 를 그대로 재사용해 3상태·도착·기준일 로직 중복을 피한다. 지도는 기본 접힘(`showMap=false`)이라 테스트에서 Leaflet 이 렌더되지 않는다.

- [ ] **Step 1: 실패하는 테스트 작성** — `app/src/features/citizen/ElderHome.test.tsx`

```tsx
import { describe, it, expect, beforeEach } from "vitest";
import { render, screen, fireEvent, within } from "@testing-library/react";
import { MemoryRouter } from "react-router-dom";
import ElderHome from "./ElderHome";
import { useStops } from "../../store/useStops";
import { useFavorites } from "../../store/useFavorites";
import { useViewMode } from "../../store/useViewMode";
import type { Stop } from "../../types/stop";

const mk = (id: string, name: string, lat: number, lng: number): Stop => ({
  id,
  stopNo: id,
  name,
  lat,
  lng,
  routes: ["1"],
  facilities: {
    shade: { status: "unknown", source: "none" },
    seat: { status: "yes", source: "bench_registry" },
    light: { status: "unknown", source: "none" },
    sign: { status: "unknown", source: "none" },
  },
  headwayMin: 10,
});

beforeEach(() => {
  localStorage.clear();
  useFavorites.setState({ ids: [] });
  useViewMode.setState({ mode: "elder" });
  useStops.setState({
    stops: [
      mk("A", "가까운정류장", 37.8813, 127.73),
      mk("B", "먼정류장", 37.95, 127.8),
      mk("C", "중간정류장", 37.882, 127.731),
    ],
    loaded: true,
  });
});

const renderHome = () =>
  render(
    <MemoryRouter>
      <ElderHome />
    </MemoryRouter>,
  );

describe("ElderHome", () => {
  it("즐겨찾기가 없으면 최근접 정류장을 주 카드로 보인다", () => {
    renderHome();
    expect(screen.getByText("가장 가까운 정류장")).toBeInTheDocument();
    expect(
      screen.getByRole("heading", { level: 2, name: /가까운정류장/ }),
    ).toBeInTheDocument();
  });

  it("즐겨찾기가 있으면 그 정류장을 주 카드로 보인다", () => {
    useFavorites.setState({ ids: ["B"] });
    renderHome();
    expect(screen.getByText("⭐ 내 정류장")).toBeInTheDocument();
    expect(
      screen.getByRole("heading", { level: 2, name: /먼정류장/ }),
    ).toBeInTheDocument();
  });

  it("근처 목록에서 다른 정류장을 누르면 주 카드가 바뀐다", () => {
    renderHome();
    const list = screen.getByRole("region", { name: "근처 정류장" });
    fireEvent.click(within(list).getByText("중간정류장"));
    expect(
      screen.getByRole("heading", { level: 2, name: /중간정류장/ }),
    ).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd app && npx vitest run src/features/citizen/ElderHome.test.tsx`
Expected: FAIL — 모듈 해석 실패.

- [ ] **Step 3: 구현** — `app/src/features/citizen/ElderHome.tsx`

```tsx
// 어른용 첫 화면 — "내 정류장"(즐겨찾기 우선, 없으면 최근접) 큰 카드 + 근처 목록.
// 지도는 기본 접힘. 정류장 선택은 지도 탭 대신 큰 목록 탭.

import { useEffect, useMemo, useState } from "react";
import { Link } from "react-router-dom";
import StopCard from "./StopCard";
import MapView from "../map/MapView";
import VersionToggle from "../../components/VersionToggle";
import { useStops } from "../../store/useStops";
import { useFavorites } from "../../store/useFavorites";
import { facilitySummary } from "../../lib/facilityText";
import { haversine, type LatLng } from "../../lib/geo";
import { CITY_CENTER } from "../../types/stop";
import type { Stop } from "../../types/stop";
import { resolvePrimaryStop, nearbyStops } from "./elderHome";
import "./ElderHome.css";

export default function ElderHome() {
  const stops = useStops((s) => s.stops);
  const loaded = useStops((s) => s.loaded);
  const favIds = useFavorites((s) => s.ids);
  const [pos, setPos] = useState<LatLng>(CITY_CENTER);
  const [override, setOverride] = useState<string | null>(null);
  const [showMap, setShowMap] = useState(false);

  // 현위치(거부/미지원이면 시청 좌표 유지 — 무한대기 없음).
  useEffect(() => {
    if (typeof navigator === "undefined" || !navigator.geolocation) return;
    navigator.geolocation.getCurrentPosition(
      (p) => setPos({ lat: p.coords.latitude, lng: p.coords.longitude }),
      () => {},
      { enableHighAccuracy: true, timeout: 6000, maximumAge: 60000 },
    );
  }, []);

  const primary = useMemo<Stop | null>(() => {
    if (override) {
      const o = stops.find((s) => s.id === override);
      if (o) return o;
    }
    return resolvePrimaryStop(favIds, stops, pos);
  }, [override, favIds, stops, pos]);

  const nearby = useMemo<Stop[]>(
    () => (primary ? nearbyStops(stops, pos, 5, primary.id) : []),
    [stops, pos, primary],
  );

  return (
    <main className="elderhome">
      <header className="elderhome__bar">
        <div className="elderhome__brand">
          <span className="elderhome__logo" aria-hidden="true">
            ,
          </span>
          <span className="elderhome__title">쉼표 정류장</span>
        </div>
        <VersionToggle />
      </header>

      {!loaded ? (
        <p className="elderhome__msg">정류장 정보를 불러오는 중…</p>
      ) : primary ? (
        <>
          <p className="elderhome__label">
            {favIds.includes(primary.id) ? "⭐ 내 정류장" : "가장 가까운 정류장"}
          </p>

          <StopCard stop={primary} />

          <Link className="elderhome__go" to="/go">
            버스 타러 가기 →
          </Link>

          {nearby.length > 0 && (
            <section className="elderhome__nearby" aria-label="근처 정류장">
              <h2 className="elderhome__nearby-title">근처 다른 정류장</h2>
              <ul className="elderhome__list">
                {nearby.map((s) => (
                  <li key={s.id}>
                    <button
                      type="button"
                      className="elderhome__item"
                      onClick={() => setOverride(s.id)}
                    >
                      <span className="elderhome__item-name">{s.name}</span>
                      <span className="elderhome__item-dist">
                        {Math.round(haversine(pos, { lat: s.lat, lng: s.lng }))}m
                      </span>
                      <span className="elderhome__item-sum">
                        {facilitySummary(s)}
                      </span>
                    </button>
                  </li>
                ))}
              </ul>
            </section>
          )}

          <button
            type="button"
            className="elderhome__mapbtn"
            onClick={() => setShowMap((v) => !v)}
            aria-expanded={showMap}
          >
            {showMap ? "지도 접기" : "지도 보기"}
          </button>
          {showMap && (
            <div className="elderhome__map">
              <MapView onSelect={(s) => setOverride(s.id)} selectedId={primary.id} />
            </div>
          )}
        </>
      ) : (
        <p className="elderhome__msg">정류장 정보를 찾을 수 없어요.</p>
      )}
    </main>
  );
}
```

`app/src/features/citizen/ElderHome.css`:

```css
.elderhome {
  min-height: 100dvh;
  padding: var(--sp-3) var(--sp-3) var(--sp-7);
  display: flex;
  flex-direction: column;
  gap: var(--sp-3);
}
.elderhome__bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--sp-3);
  padding: var(--sp-2) var(--sp-2) var(--sp-1);
}
.elderhome__brand {
  display: inline-flex;
  align-items: center;
  gap: var(--sp-2);
}
.elderhome__logo {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 34px;
  height: 34px;
  border-radius: 50%;
  background: var(--brand);
  color: var(--on-brand);
  font-size: 34px;
  font-weight: 900;
  line-height: 0.6;
  padding-bottom: 12px;
}
.elderhome__title {
  font-size: var(--fs-md);
  font-weight: 800;
  color: var(--ink);
}
.elderhome__label {
  font-size: var(--fs-lg);
  font-weight: 800;
  color: var(--brand-strong);
  padding: 0 var(--sp-2);
}
.elderhome__msg {
  font-size: var(--fs-md);
  font-weight: 700;
  text-align: center;
  padding: var(--sp-6) var(--sp-4);
}
/* "버스 타러 가기" — 어른용 주행동 큰 버튼 */
.elderhome__go {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: var(--touch-lg);
  border-radius: var(--radius-lg);
  background: var(--warm);
  color: var(--on-warm);
  font-size: var(--fs-lg);
  font-weight: 800;
  text-decoration: none;
  box-shadow: var(--shadow-card);
}
.elderhome__go:active {
  background: var(--warm-strong);
}
.elderhome__nearby-title {
  font-size: var(--fs-md);
  font-weight: 800;
  color: var(--ink);
  padding: var(--sp-2) var(--sp-2) 0;
}
.elderhome__list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: var(--sp-2);
}
.elderhome__item {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: var(--sp-1) var(--sp-3);
  width: 100%;
  min-height: var(--touch-lg);
  padding: var(--sp-3) var(--sp-4);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: var(--surface);
  text-align: left;
  cursor: pointer;
  box-shadow: var(--shadow-card);
}
.elderhome__item:active {
  background: var(--surface-2);
}
.elderhome__item-name {
  font-size: var(--fs-md);
  font-weight: 800;
  color: var(--ink);
}
.elderhome__item-dist {
  font-size: var(--fs-base);
  font-weight: 700;
  color: var(--ink-muted);
  align-self: center;
}
.elderhome__item-sum {
  grid-column: 1 / -1;
  font-size: var(--fs-sm);
  color: var(--ink-body);
}
.elderhome__mapbtn {
  min-height: var(--touch);
  border: 2px solid var(--border-strong);
  border-radius: var(--radius-pill);
  background: var(--surface);
  color: var(--ink-body);
  font-size: var(--fs-base);
  font-weight: 800;
  cursor: pointer;
}
.elderhome__map {
  height: 60dvh;
  border-radius: var(--radius);
  overflow: hidden;
}
```

- [ ] **Step 4: 통과 확인**

Run: `cd app && npx vitest run src/features/citizen/ElderHome.test.tsx`
Expected: PASS (3 tests). (경고로 geolocation/fetch 관련 로그가 나와도 통과면 OK.)

- [ ] **Step 5: 커밋**

```bash
git add app/src/features/citizen/ElderHome.tsx app/src/features/citizen/ElderHome.css app/src/features/citizen/ElderHome.test.tsx
git commit -m "feat: 어른용 첫 화면 ElderHome(내 정류장 큰 카드 + 근처 목록)"
```

---

### Task 6: 라우팅 분기(`CitizenRoot`) + App 셸 적용 + 일반 홈에 토글

**Files:**
- Create: `app/src/features/citizen/CitizenRoot.tsx`
- Test: `app/src/features/citizen/CitizenRoot.test.tsx`
- Modify: `app/src/App.tsx`, `app/src/features/citizen/CitizenHome.tsx`

**Interfaces:**
- Consumes: `useViewMode` (Task 1), `ElderHome` (Task 5), `CitizenHome`(기존), `ShellLayout` (Task 3), `VersionToggle` (Task 2).
- Produces: `CitizenRoot` default export — `mode==="elder" ? <ElderHome/> : <CitizenHome/>`.

- [ ] **Step 1: 실패하는 테스트 작성** — `app/src/features/citizen/CitizenRoot.test.tsx`

```tsx
import { describe, it, expect, beforeEach, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import { MemoryRouter } from "react-router-dom";
import { useViewMode } from "../../store/useViewMode";
import { useStops } from "../../store/useStops";
import { useFavorites } from "../../store/useFavorites";
import type { Stop } from "../../types/stop";

// Leaflet 은 jsdom 에서 무겁고 불안정 → MapView 를 가짜로 대체.
vi.mock("../map/MapView", () => ({
  default: () => <div data-testid="mapview">지도</div>,
}));

import CitizenRoot from "./CitizenRoot";

const mk = (id: string, name: string): Stop => ({
  id,
  stopNo: id,
  name,
  lat: 37.8813,
  lng: 127.73,
  routes: ["1"],
  facilities: {
    shade: { status: "unknown", source: "none" },
    seat: { status: "unknown", source: "none" },
    light: { status: "unknown", source: "none" },
    sign: { status: "unknown", source: "none" },
  },
  headwayMin: 10,
});

beforeEach(() => {
  localStorage.clear();
  useFavorites.setState({ ids: [] });
  useStops.setState({ stops: [mk("A", "가까운정류장")], loaded: true });
});

const renderRoot = () =>
  render(
    <MemoryRouter>
      <CitizenRoot />
    </MemoryRouter>,
  );

describe("CitizenRoot", () => {
  it("elder 모드면 어른용 홈을 렌더한다", () => {
    useViewMode.setState({ mode: "elder" });
    renderRoot();
    expect(screen.getByText("가장 가까운 정류장")).toBeInTheDocument();
  });

  it("normal 모드면 일반(지도) 홈을 렌더한다", () => {
    useViewMode.setState({ mode: "normal" });
    renderRoot();
    expect(screen.getByTestId("mapview")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd app && npx vitest run src/features/citizen/CitizenRoot.test.tsx`
Expected: FAIL — `CitizenRoot` 모듈 해석 실패.

- [ ] **Step 3: `CitizenRoot` 구현** — `app/src/features/citizen/CitizenRoot.tsx`

```tsx
import { useViewMode } from "../../store/useViewMode";
import CitizenHome from "./CitizenHome";
import ElderHome from "./ElderHome";

// 첫 화면 라우트 — 버전에 따라 어른용/일반 홈을 고른다.
export default function CitizenRoot() {
  const mode = useViewMode((s) => s.mode);
  return mode === "elder" ? <ElderHome /> : <CitizenHome />;
}
```

- [ ] **Step 4: 일반 홈 상단바에 토글 추가** — `app/src/features/citizen/CitizenHome.tsx`

파일 상단 import 에 추가:

```tsx
import VersionToggle from "../../components/VersionToggle";
```

`home__actions` 의 첫 자식으로 토글을 넣는다. 아래 라인:

```tsx
        <div className="home__actions">
          <Link className="home__go" to="/go">
```

을 다음으로 바꾼다:

```tsx
        <div className="home__actions">
          <VersionToggle />
          <Link className="home__go" to="/go">
```

- [ ] **Step 5: `App.tsx` 셸 레이아웃 + 라우팅 교체** — `app/src/App.tsx` 전체를 아래로 교체

```tsx
import { useEffect } from "react";
import { BrowserRouter, Routes, Route } from "react-router-dom";
import ShellLayout from "./components/ShellLayout";
import CitizenRoot from "./features/citizen/CitizenRoot";
import Favorites from "./features/citizen/Favorites";
import TripView from "./features/trip/TripView";
import PrintPoster from "./features/print/PrintPoster";
import Dashboard from "./features/admin/Dashboard";
import { useStops } from "./store/useStops";

export default function App() {
  const load = useStops((s) => s.load);
  const loaded = useStops((s) => s.loaded);

  useEffect(() => {
    if (!loaded) {
      void load();
    }
  }, [load, loaded]);

  return (
    <BrowserRouter>
      <Routes>
        {/* 시민 화면 — 폰 폭 셸 안 */}
        <Route element={<ShellLayout />}>
          <Route path="/" element={<CitizenRoot />} />
          <Route path="/favorites" element={<Favorites />} />
          <Route path="/go" element={<TripView />} />
        </Route>
        {/* 행정·인쇄 — 셸 밖(손대지 않음) */}
        <Route path="/print/:id" element={<PrintPoster />} />
        <Route path="/admin" element={<Dashboard />} />
      </Routes>
    </BrowserRouter>
  );
}
```

- [ ] **Step 6: 통과 확인 + 회귀 확인**

Run: `cd app && npx vitest run src/features/citizen/CitizenRoot.test.tsx`
Expected: PASS (2 tests).

- [ ] **Step 7: 커밋**

```bash
git add app/src/features/citizen/CitizenRoot.tsx app/src/features/citizen/CitizenRoot.test.tsx app/src/features/citizen/CitizenHome.tsx app/src/App.tsx
git commit -m "feat: 버전별 첫 화면 분기(CitizenRoot) + 시민 라우트 폰 셸 적용"
```

---

### Task 7: 버스 타러/즐겨찾기 버전 반영 + 최종 검증

**Files:**
- Modify: `app/src/features/trip/TripView.tsx`, `app/src/features/trip/TripView.css`, `app/src/features/citizen/Favorites.tsx`, `app/src/features/citizen/Favorites.css`
- Test: `app/src/features/trip/TripView.modeAttr.test.tsx`

**Interfaces:**
- Consumes: `useViewMode` (Task 1), `VersionToggle` (Task 2).
- Produces: 없음(기존 화면에 `data-mode` 속성 + 토글 추가, 어른용 CSS 확대).

**설계 메모:** 로직은 불변. 루트 요소에 `data-mode={mode}` 를 달고, 어른용일 때 CSS 로 제목·항목 글씨를 키운다. `Favorites` 는 이미 초대형 카드라 토글·`data-mode` 만 추가한다.

- [ ] **Step 1: 실패하는 테스트 작성** — `app/src/features/trip/TripView.modeAttr.test.tsx`

```tsx
import { describe, it, expect, beforeEach } from "vitest";
import { render } from "@testing-library/react";
import { MemoryRouter } from "react-router-dom";
import TripView from "./TripView";
import { useStops } from "../../store/useStops";
import { useFavorites } from "../../store/useFavorites";
import { useViewMode } from "../../store/useViewMode";

beforeEach(() => {
  localStorage.clear();
  useFavorites.setState({ ids: [] });
  useStops.setState({ stops: [], loaded: true });
});

const renderTrip = () =>
  render(
    <MemoryRouter>
      <TripView />
    </MemoryRouter>,
  );

describe("TripView 버전 속성", () => {
  it("어른용이면 루트에 data-mode='elder' 를 단다", () => {
    useViewMode.setState({ mode: "elder" });
    const { container } = renderTrip();
    expect(container.querySelector("main.tripview")?.getAttribute("data-mode")).toBe("elder");
  });

  it("일반이면 data-mode='normal' 이다", () => {
    useViewMode.setState({ mode: "normal" });
    const { container } = renderTrip();
    expect(container.querySelector("main.tripview")?.getAttribute("data-mode")).toBe("normal");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd app && npx vitest run src/features/trip/TripView.modeAttr.test.tsx`
Expected: FAIL — `data-mode` 가 null.

- [ ] **Step 3: `TripView` 에 버전 속성/토글 추가** — `app/src/features/trip/TripView.tsx`

상단 import 에 추가:

```tsx
import VersionToggle from "../../components/VersionToggle";
import { useViewMode } from "../../store/useViewMode";
```

컴포넌트 본문 맨 위(`const stops = ...` 위)에 추가:

```tsx
  const mode = useViewMode((s) => s.mode);
```

루트 `<main className="tripview">` 를 다음으로 교체:

```tsx
    <main className="tripview" data-mode={mode}>
```

헤더의 `<span className="tripview__spacer" aria-hidden="true" />` 를 토글로 교체:

```tsx
        <VersionToggle />
```

- [ ] **Step 4: `TripView.css` 어른용 확대 규칙 추가** — `app/src/features/trip/TripView.css` 끝에 추가

```css
/* 어른용: 목적지 이름·안내 글씨 확대 */
.tripview[data-mode="elder"] .tripview__title {
  font-size: var(--fs-xl);
}
.tripview[data-mode="elder"] .tripview__dests-label {
  font-size: var(--fs-lg);
}
.tripview[data-mode="elder"] .tripview__dest {
  min-height: var(--touch-lg);
  font-size: var(--fs-md);
}
```

- [ ] **Step 5: `Favorites` 에 버전 속성/토글 추가** — `app/src/features/citizen/Favorites.tsx`

상단 import 에 추가:

```tsx
import VersionToggle from "../../components/VersionToggle";
import { useViewMode } from "../../store/useViewMode";
```

`export default function Favorites() {` 바로 아래에 추가:

```tsx
  const mode = useViewMode((s) => s.mode);
```

루트 `<main className="favpage">` 를 교체:

```tsx
    <main className="favpage" data-mode={mode}>
```

헤더의 `<span className="favpage__spacer" aria-hidden="true" />` 를 토글로 교체:

```tsx
        <VersionToggle />
```

- [ ] **Step 6: `Favorites.css` 어른용 확대 규칙 추가** — `app/src/features/citizen/Favorites.css` 끝에 추가

```css
/* 어른용: 정류장 이름 확대 */
.favpage[data-mode="elder"] .favcard__name {
  font-size: var(--fs-xl);
}
.favpage[data-mode="elder"] .favpage__title {
  font-size: var(--fs-xl);
}
```

- [ ] **Step 7: 이 태스크 테스트 통과 확인**

Run: `cd app && npx vitest run src/features/trip/TripView.modeAttr.test.tsx`
Expected: PASS (2 tests).

- [ ] **Step 8: 전체 회귀 — 테스트·린트·빌드**

```bash
cd app
npm test
npm run lint
npm run build
```
Expected: 테스트 전부 PASS, lint 0 error, build 성공.

- [ ] **Step 9: 실제 앱 확인(수동)**

```bash
cd app && npm run dev
```
브라우저 http://localhost:5173 에서 확인:
1. 첫 진입이 어른용(내 정류장/최근접 큰 카드 + 근처 목록)인가.
2. 상단 "일반" 누르면 지도 중심으로 바뀌고, "큰글씨" 누르면 되돌아오는가.
3. 새로고침해도 마지막 버전이 유지되는가(localStorage).
4. 데스크톱에서 폰 폭으로 보이는가. `/admin` 은 예전처럼 넓게 나오는가.

- [ ] **Step 10: 커밋**

```bash
git add app/src/features/trip/TripView.tsx app/src/features/trip/TripView.css app/src/features/trip/TripView.modeAttr.test.tsx app/src/features/citizen/Favorites.tsx app/src/features/citizen/Favorites.css
git commit -m "feat: 버스 타러/즐겨찾기 버전 토글·어른용 확대 + 최종 검증"
```

---

## 자기검토 결과

- **스펙 커버리지:** 뷰모드 스토어(T1)·토글(T2)·모바일 셸(T3)·어른용 로직(T4)·어른용 홈(T5)·라우팅 분기와 셸 적용(T6)·버스타러/즐겨찾기 반영 및 검증(T7) → 스펙의 모든 요구 반영. 행정/인쇄는 셸 밖(T6)으로 명시적으로 제외.
- **Placeholder 스캔:** 없음. 모든 코드·명령·기대출력 명시.
- **타입 일관성:** `resolvePrimaryStop`/`nearbyStops` 시그니처가 T4 정의와 T5 사용에서 동일. `useViewMode` 의 `mode/setMode/toggle` 이 T1 정의와 T2·T5·T6·T7 사용에서 동일. `VersionToggle`·`ShellLayout`·`AppShell` 프롭 일치.
- **테스트 격리:** Leaflet 회피 — ElderHome 은 `showMap=false` 기본이라 MapView 미렌더, CitizenRoot 는 `vi.mock` 으로 MapView 대체.
